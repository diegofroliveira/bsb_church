# C4 do IgrejaPro

Esta visão representa a arquitetura-alvo inicial. Elementos de billing e provedores externos são capacidades planejadas, não indicação de integração já implementada.

## Nível 1 — Contexto

```mermaid
flowchart LR
    member["Membro ou participante"]
    leader["Líder, discipulador ou voluntário"]
    staff["Pastor, funcionário ou administrador"]
    network["Gestor de denominação ou ministério"]
    prover["Prover legado — fonte temporária"]
    channels["E-mail, mensageria e pagamentos"]
    system["IgrejaPro SaaS"]

    member -->|"consulta agenda, dados e solicitações"| system
    leader -->|"acompanha pessoas, grupos e escalas"| system
    staff -->|"administra organização, cadastro e finanças"| system
    network -->|"governa unidades conforme grants"| system
    prover -->|"exportação temporária e controlada"| system
    system -->|"envios e transações autorizadas"| channels
```

## Nível 2 — Containers

```mermaid
flowchart TB
    users["Usuários web e mobile"]
    admin["Operadores administrativos"]

    subgraph product["IgrejaPro"]
        web["Web App\nReact + TypeScript\nUX, rotas e estado de sessão"]
        api["API e funções server-side\nComandos privilegiados, validação e integrações"]
        auth["Supabase Auth\nIdentidade e sessão"]
        db[("PostgreSQL\nDomínios, RLS, views e auditoria")]
        storage["Object Storage\nAnexos privados e imagens"]
        jobs["Workers e jobs\nImportação, outbox, notificações e métricas"]
        analytics["Camada analítica\nViews e snapshots tenant-aware"]
    end

    external["Serviços externos\nProver temporário, comunicação, pagamentos e IA"]

    users -->|"HTTPS"| web
    admin -->|"HTTPS + MFA quando disponível"| web
    web -->|"login e refresh"| auth
    web -->|"JWT do usuário; queries autorizadas"| db
    web -->|"HTTPS; comandos"| api
    api -->|"sessão validada; transações"| db
    api -->|"URLs assinadas e políticas"| storage
    jobs -->|"service identity restrita"| db
    jobs -->|"conectores idempotentes"| external
    api -->|"integrações autorizadas"| external
    db --> analytics
    web -->|"consultas no escopo"| analytics
```

## Limites e responsabilidades

- Web App não decide autorização final e nunca recebe service role.
- API é obrigatória para administração de usuários, grants, operações financeiras sensíveis, exportações e integrações.
- PostgreSQL é a última barreira de isolamento por meio de RLS forçado.
- Jobs usam identidades e permissões mínimas, com idempotência, correlação e auditoria.
- Storage é privado por padrão; acesso ocorre por policy ou URL assinada de curta duração.
- Analytics preserva `tenant_id`, escopo organizacional e regras de sensibilidade.
